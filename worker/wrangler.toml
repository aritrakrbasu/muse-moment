name = "muse-moment-gemini-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# Environment variables (set via: npx wrangler secret put GEMINI_API_KEY)
# GEMINI_API_KEY = "your-api-key-here"

# Optional: Add your domain for additional security
# routes = [
#   { pattern = "api.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]
